Метод двух указателей -
    применяется для оптимизации задач решаемых вложенными циклами
    в итоге дает выигрыш в сложности O(n) vs O(n²)

    разобранные задачи:
1. Таргетная сумма
    -отсортировать массив
    -1 указатель - самый левый, 2ой - самый правый, пока не сойдутся навстречу
2. Минимальная разность между элементами
    -сортировка
    -проверка рядомстоящих пар начиная с первой
    -1 указатель - 0ой элемент, 2 указтель - 1ый, пока правый не уткнется в край массива
4. Проверка на палиндром
    - левый + правый указатели
    - проходим ими до встречи - если на каком то этапе элементы не равны - не палиндром
5. Провекра на палиндром LeetCode
    -тоже самое но, нужно приводить буквы к нижнему написанию Character.toLowerCase()
    -убирать проходящими while-циклами все не буквы и не цифры !Character.isLetterOrDigit()
    -преобразовать строку в список символов String s = "..."; char[] ss = toCharArray()

В качестве экзамена для себя - должен